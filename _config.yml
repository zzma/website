title: Zane Ma
description: "Zane Ma"
sourcecode: "https://github.com/zzma/website"

permalink: blog/:year/:month/:day/:title
exclude: ["Makefile", "README.md", "vendor"]

url: "https://zanema.com"     # the base hostname & protocol for your site
baseurl: ""                   # the subpath of your site, e.g. /blog

# Build settings
markdown: kramdown
highlighter: rouge

# Settings for analytics helper
# Set 'provider' to the analytics provider you want to use.
# Set 'provider' to false to turn analytics off globally.
#
analytics:
    provider:
    # statcounter:
    #     sc_project  :
    #     sc_security :
    #     sc_invisible: 1
    #     sc_text     : 2
    google:
        tracking_id: 'UA-102239201-1'
    # getclicky:
    #   site_id:
    # mixpanel:
    #     token: ''
    # piwik:
    #     baseURL: '' # Piwik installation address (without protocol)
    #     idsite: ''  # the id of the site on Piwik